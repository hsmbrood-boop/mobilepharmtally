<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 공유 저장소(예: /storage/emulated/0/OneSyncFiles) 의 임의 폴더에
         엑셀을 직접 읽고/쓰기 위해 필요한 권한들.
         - Android 10 이하: READ/WRITE_EXTERNAL_STORAGE + requestLegacyExternalStorage
         - Android 11+    : MANAGE_EXTERNAL_STORAGE (사용자가 설정에서 "모든 파일 접근" 허용 필요) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- 새 엑셀 파일이 들어왔을 때 로컬 알림을 띄우기 위함. Android 13+ 에서는
         사용자 동의가 필요한 런타임 권한. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 폰을 재부팅해도 폴더 감시 작업이 다시 등록되도록 (WorkManager
         내부 BootReceiver 가 이 권한을 사용). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 배터리 최적화(도즈) 때문에 15분 주기 폴더 감시가 밤에 1~2시간씩
         밀리는 것을 줄이기 위해, 사용자에게 "최적화 제외"를 요청한다.
         (Google Play 는 이 권한 사용을 제한하지만 ONE Store 배포에는 무방.) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:label="PharmTally"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
